---
wave: 2
depends_on:
  - "04-01"
files_modified:
  - multi-tunnel-namespace/src/daemon/totp_service.py
  - multi-tunnel-namespace/pyproject.toml
  - multi-tunnel-namespace/src/daemon/daemon.py
autonomous: false
must_haves:
  truths:
    - "TOTP service client module exists with health_check and secret retrieval"
    - "Daemon fetches TOTP secret via totp_service when totp_enabled is True"
  artifacts:
    - path: src/daemon/totp_service.py
      provides: "TOTP service client with async health_check and get_user_totp_secret"
      min_lines: 80
    - path: pyproject.toml
      provides: "keyring dependency and TOTP service configuration"
      min_lines: 1
    - path: src/daemon/daemon.py (_spawn_adapter)
      provides: "Logic to call totp_service.get_user_totp_secret and include totp_secret in startup payload"
      min_lines: 5
  key_links:
    - from: daemon.totp_enabled
      to: totp_service.health_check()
      via: "health check result sets totp_enabled at startup"
    - from: _spawn_adapter
      to: totp_service.get_user_totp_secret()
      via: "fetches secret per adapter spawn when totp_enabled"
---

<tasks>
<task id="1" type="auto">
<description>Create TOTP service client with health check and secret retrieval</description>
<read_first>
- .planning/phases/04-polish-security-compatibility/04-CONTEXT.md (TOTP service integration decision)
- .planning/phases/04-polish-security-compatibility/04-RESEARCH.md (implementation notes)
</read_first>
<action>
1. Create file `multi-tunnel-namespace/src/daemon/totp_service.py`.
2. Define custom exception classes inheriting from a base `TOTPServiceError`:
   - `TOTPServiceUnavailable` (network failure, timeout)
   - `TOTPAuthFailed` (invalid credentials to service)
   - `UserNotEnrolled` (user has no 2FA setup)
   - `TOTPRateLimited` (rate limit exceeded; include retry_after)
3. Define `async def health_check(timeout: float = 5.0) -> bool`:
   - Read configured service URL from env `PROTONVPN_TOTP_SERVICE_URL` or config file `/etc/protonvpn/totp-service.conf`
   - Perform lightweight GET to `/health` endpoint (or POST with test challenge)
   - Return True if 200 OK with expected JSON payload; False otherwise
4. Define `async def get_user_totp_secret(username: str) -> bytes`:
   - Use aiohttp or httpx (to be implemented; stub version raises NotImplementedError)
   - Retrieve session token from system keyring (libsecret) using stored credentials
   - POST to `/v2/2fa/secret` endpoint (from doc: 2FA_AUTHENTICATION.md)
   - Response JSON: `{"secret": "BASE64...", "method": "totp"}`
   - Return base64-decoded raw bytes
   - Raise appropriate exceptions on error
   - Rate limiting: track per-user attempts; max 3/min; exponential backoff (max 3 retries)
5. Module docstring explains: replaces `--totp` flag; TOTP mode auto-detected via health check.
6. Stub implementation: for now, body of `get_user_totp_secret` raises `NotImplementedError("TOTP service integration stub")`; `health_check` returns False (service not implemented).
</action>
<acceptance_criteria>
- File `src/daemon/totp_service.py` exists with ~80+ lines.
- Contains `async def health_check(timeout: float = 5.0) -> bool`.
- Contains `async def get_user_totp_secret(username: str) -> bytes`.
- Defines at least 5 exception classes: base + 4.
- Function body of `get_user_totp_secret` raises `NotImplementedError` with proper message.
- `health_check` reads configuration and returns False (stub) or raises config error.
- Module has comprehensive docstring.
</acceptance_criteria>
</task>

<task id="2" type="auto">
<description>Add keyring dependency and TOTP service configuration to pyproject.toml</description>
<read_first>
- multi-tunnel-namespace/pyproject.toml
</read_first>
<action>
1. Under `[project]` `dependencies`, add `"keyring>=24.0"` if not present.
2. Add a `[tool.mtm]` section (or extend if exists) with:
   - `totp_service_url` (placeholder, e.g., `"https://totp.example.com"` or can be a list)
   - Optional: `totp_rate_limit = 3` and `rate_limit_window = 60`
   - Optional: `totp_health_timeout = 5.0`
3. Ensure the file remains valid TOML.
</action>
<acceptance_criteria>
- `pyproject.toml` contains `"keyring>=24.0"` in dependencies.
- `[tool.mtm]` section exists with at least `totp_service_url` key.
</acceptance_criteria>
</task>

<task id="3" type="auto">
<description>Integrate TOTP service fetch into adapter spawn (daemon._spawn_adapter)</description>
<read_first>
- multi-tunnel-namespace/src/daemon/daemon.py (VPNDaemon._spawn_adapter)
- multi-tunnel-namespace/src/daemon/daemon.py (VPNDaemon.__init__ and class attributes)
</read_first>
<action>
1. Ensure VPNDaemon class has `self.totp_enabled: bool = False` set in `__init__` (from 04-01). No constructor parameter; it's set by main() after health check.
2. In `_spawn_adapter`, after getting `vpn_username` and before constructing `startup_payload`:
   ```python
   totp_secret = None
   if self.totp_enabled:
       try:
           raw_secret = await totp_service.get_user_totp_secret(vpn_username)
           import base64
           totp_secret = base64.b64encode(raw_secret).decode('ascii')
       except totp_service.TOTPServiceError as e:
           logger.warning(f"TOTP secret fetch failed for {vpn_username}: {e}; proceeding without TOTP")
       except Exception as e:
           logger.warning(f"Unexpected error fetching TOTP secret: {e}; proceeding without TOTP")
   ```
3. Add `"totp_secret": totp_secret` to `startup_payload`.
4. Ensure imports: `import base64` at top; `from . import totp_service` (or absolute import).
5. Keep existing logging of adapter spawn.
</action>
<acceptance_criteria>
- `daemon.py` imports `base64` and `totp_service`.
- `_spawn_adapter` contains conditional block using `self.totp_enabled`.
- Block calls `totp_service.get_user_totp_secret` and handles exceptions.
- `startup_payload` includes `totp_secret` key (can be None).
- No `totp_enabled` parameter in VPNDaemon.__init__; it's an instance attribute set elsewhere.
</acceptance_criteria>
</task>
</tasks>

<verification>
After this plan:
1. `proton-vpn-manager --help` shows NO `--totp` flag (removed).
2. At daemon startup, `totp_service.health_check()` is called; `daemon.totp_enabled` set accordingly.
3. When an adapter spawns, if `totp_enabled` is True, `totp_service.get_user_totp_secret` is called and result (if successful) is included in stdin payload as `totp_secret` (base64 string). If fails, `totp_secret` is None.
4. TOTP service stub raises `NotImplementedError`; daemon logs warning and continues.
</verification>