---
wave: 8
depends_on:
  - "04-07"
files_modified:
  - multi-tunnel-namespace/tests/integration/test_phase4_security.py
  - multi-tunnel-namespace/tests/integration/test_phase4_legacy_api.py
  - multi-tunnel-namespace/tests/integration/test_phase4_totp_encryption.py
autonomous: false
must_haves:
  truths:
    - "Integration test stubs exist for security, legacy API, and TOTP encryption"
    - "Each test file contains at least the required test functions with pytest.skip"
  artifacts:
    - path: tests/integration/test_phase4_security.py
      provides: "Security audit test stubs: socket permissions, /proc leakage, SO_PEERCRED"
      min_lines: 30
    - path: tests/integration/test_phase4_legacy_api.py
      provides: "Legacy D-Bus API forwarding test stubs: CreateTunnel, others"
      min_lines: 30
    - path: tests/integration/test_phase4_totp_encryption.py
      provides: "TOTP encryption test stubs: control channel and CLI↔Adapter"
      min_lines: 30
  key_links:
    - from: test_phase4_security.py
      to: SEC-01, SEC-02, SEC-03
      via: "one or more tests per requirement"
    - from: test_phase4_legacy_api.py
      to: COMP-01, COMP-02
      via: "forwarding correctness tests"
    - from: test_phase4_totp_encryption.py
      to: SEC-08
      via: "encryption verification tests"
---

<tasks>
<task id="1" type="auto">
<description>Create test_phase4_security.py with stubs for SEC-01, SEC-02, SEC-03</description>
<read_first>
- multi-tunnel-namespace/tests/integration/ (existing test patterns)
- .planning/REQUIREMENTS.md (SEC-01, SEC-02, SEC-03 definitions)
</read_first>
<action>
1. Create file `tests/integration/test_phase4_security.py`.
2. Add pytest imports and necessary fixtures (reuse `manager_client`, `daemon` from existing integration tests).
3. Define test functions with docstrings referencing the requirement IDs:
   ```python
   @pytest.mark.asyncio
   async def test_adapter_cli_socket_permissions(manager_client):
       """SEC-01: Adapter CLI socket is created with mode 0600 and owned appropriately."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_no_credentials_in_proc(manager_client):
       """SEC-02: Adapter process environment and /proc/pid/environ do not contain VPN credentials."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_control_socket_peer_cred(daemon):
       """SEC-03: Control socket uses SO_PEERCRED to reject unauthorized connections."""
       pytest.skip("Implementation pending after all phases")
   ```
4. Save the file.
</action>
<acceptance_criteria>
- File exists with three test functions as above.
- Each uses `pytest.skip("Implementation pending after all phases")`.
- Each references the correct SEC requirement in docstring.
- Imports and fixtures are consistent with existing integration test suite.
</acceptance_criteria>
</task>

<task id="2" type="auto">
<description>Create test_phase4_legacy_api.py with stubs for COMP-01, COMP-02</description>
<read_first>
- multi-tunnel-namespace/tests/integration/ (existing test patterns)
- .planning/REQUIREMENTS.md (COMP-01, COMP-02 definitions)
</read_first>
<action>
1. Create file `tests/integration/test_phase4_legacy_api.py`.
2. Define test functions:
   ```python
   @pytest.mark.asyncio
   async def test_legacy_create_tunnel_forwards_to_adapter(manager_client):
       """COMP-01: Legacy D-Bus CreateTunnel forwards to adapter transparently."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_legacy_destroy_tunnel(manager_client):
       """COMP-02: Legacy DestroyTunnel works via forwarding."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_legacy_other_methods_forward(manager_client):
       """COMP-02 also: ConnectTunnel, DisconnectTunnel, ListTunnels, GetTunnelStatus."""
       pytest.skip("Implementation pending after all phases")
   ```
3. Ensure fixtures are imported correctly (e.g., `from tests.integration.fixtures import manager_client, daemon`).
4. Save file.
</action>
<acceptance_criteria>
- File exists with at least three test functions covering COMP-01 and COMP-02.
- Each has `pytest.skip`.
- Fixtures are compatible.
</acceptance_criteria>
</task>

<task id="3" type="auto">
<description>Create test_phase4_totp_encryption.py with stubs for SEC-08</description>
<read_first>
- multi-tunnel-namespace/tests/integration/ (existing test patterns)
- .planning/REQUIREMENTS.md (SEC-08 definition)
</read_first>
<action>
1. Create file `tests/integration/test_phase4_totp_encryption.py`.
2. Define test functions:
   ```python
   @pytest.mark.asyncio
   async def test_control_channel_encryption(manager_client):
       """SEC-08: Control channel messages between MTM and adapter are encrypted when TOTP enabled."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_cli_adapter_encryption(manager_client):
       """SEC-08: CLI↔Adapter messages are encrypted when TOTP enabled."""
       pytest.skip("Implementation pending after all phases")

   @pytest.mark.asyncio
   async def test_encryption_with_totp_disabled(manager_client):
       """SEC-08: When TOTP disabled (service unreachable), encryption not used, messages plaintext."""
       pytest.skip("Implementation pending after all phases")
   ```
3. Save file.
</action>
<acceptance_criteria>
- File exists with at least three test functions referencing SEC-08.
- Each skips with appropriate message.
- Fixtures consistent.
</acceptance_criteria>
</task>

<task id="4" type="auto">
<description>Ensure TST-01, TST-02, TST-03 are represented in stubs</description>
<read_first>
- multi-tunnel-namespace/.planning/REQUIREMENTS.md (TST-01, TST-02, TST-03 definitions)
</read_first>
<action>
1. Verify that the three test files collectively cover:
   - **TST-01**: Full integration flow (end-to-end). The phase 1-3 already have full-flow tests? But we need a Phase 4 version that includes TOTP mode and legacy API. We can add a test in `test_phase4_security.py` or a separate file. For simplicity, add a test function `test_full_integration_with_totp` in one of these files.
   - **TST-02**: Crash recovery — test that if adapter crashes during active tunnel, MTM detects and cleans namespace. Add a test function `test_adapter_crash_cleanup` in `test_phase4_security.py` or create a new `test_phase4_crash_recovery.py`. Given stubs, we can put it in `test_phase4_security.py`.
   - **TST-03**: Concurrent access — multiple CLI processes connecting to same adapter simultaneously. Add `test_concurrent_adapter_access` in `test_phase4_security.py`.
2. If not already present, add these additional test functions to appropriate files with `pytest.skip`.
3. Ensure each is associated with correct TST requirement in docstring.
</action>
<acceptance_criteria>
- At least one test function mentions TST-01 (full integration with TOTP/legacy).
- At least one test function mentions TST-02 (crash cleanup).
- At least one test function mentions TST-03 (concurrent access).
- All are in the three new test files or a fourth stub if needed.
</acceptance_criteria>
</task>
</tasks>

<verification>
After this plan:
1. Three new test files exist in `tests/integration/` with skeleton test functions for all Phase 4 requirements.
2. Tests for TST-01, TST-02, TST-03 are present (either in these files or a dedicated crash file).
3. All tests currently skip, providing a structure to fill after implementation.
4. Test count: at least 10 test functions across the files covering SEC-01,02,03,COMP-01,02,SEC-08,TST-01,02,03.
</verification>