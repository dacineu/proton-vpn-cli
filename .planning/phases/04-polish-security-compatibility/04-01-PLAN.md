---
wave: 1
depends_on: []
files_modified:
  - multi-tunnel-namespace/src/daemon/daemon.py
autonomous: false
must_haves:
  truths:
    - "Daemon automatically detects TOTP service availability at startup"
    - "VPNDaemon instance has totp_enabled attribute set based on service health"
  artifacts:
    - path: src/daemon/daemon.py
      provides: "VPNDaemon class with automatic TOTP mode detection"
      min_lines: 1
    - path: src/daemon/pgp_service.py
      provides: "TOTP service client with health check"
      min_lines: 50
  key_links:
    - from: daemon startup
      to: pgp_service.health_check()
      via: "calls health check to determine totp_enabled"
---

<tasks>
<task id="1" type="auto">
<description>Add TOTP service configuration and health check module</description>
<read_first>
- .planning/phases/04-polish-security-compatibility/04-CONTEXT.md (TOTP service integration decision)
- multi-tunnel-namespace/src/pyproject.toml (for config structure)
</read_first>
<action>
1. Create `src/daemon/totp_service.py` with:
   - `async def health_check(timeout: float = 5.0) -> bool`: attempts to connect to configured TOTP service URL(s), returns True if healthy
   - `async def get_user_totp_secret(username: str) -> bytes`: fetches TOTP secret (same as pgp_service stub)
   - Configuration: read from environment `PROTONVPN_TOTP_SERVICE_URL` or config file `/etc/protonvpn/totp-service.conf` (format: one URL per line)
   - Health check: perform a lightweight GET to `/health` endpoint; expect 200 OK with `{"status":"ok"}`
   - Define custom exceptions: `TOTPServiceUnavailable`, `TOTPConfigError`
   - Stub implementation: for now, just raise `NotImplementedError` but include the health_check() function that returns False (service not implemented yet)
2. Add docstring explaining that this module replaces the `--totp` flag — TOTP mode is auto-detected via health check.
</action>
<acceptance_criteria>
- File `src/daemon/totp_service.py` exists
- Contains `async def health_check() -> bool`
- Contains `async def get_user_totp_secret(username: str) -> bytes`
- Reads configuration from env or config file
- Defines at least `TOTPServiceError` base exception
</acceptance_criteria>
</task>

<task id="2" type="auto">
<description>Modify VPNDaemon to auto-detect TOTP mode at startup (no --totp flag)</description>
<read_first>
- multi-tunnel-namespace/src/daemon/daemon.py (VPNDaemon.__init__ and main)
</read_first>
<action>
1. In VPNDaemon.__init__, **remove** the `totp_enabled` parameter from the signature. Instead, add:
   ```python
   self.totp_enabled: bool = False  # will be set by main() after health check
   ```
2. In `main()` (or the entry point):
   - Remove argparse `--totp` flag handling entirely.
   - After creating the daemon instance but before starting it, call:
     ```python
     try:
         is_healthy = await totp_service.health_check()
         daemon.totp_enabled = is_healthy
         if is_healthy:
             logger.info("TOTP service reachable — TOTP mode enabled")
         else:
             logger.warning("TOTP service unreachable — operating without TOTP")
     except totp_service.TOTPConfigError as e:
         logger.error(f"TOTP service configuration error: {e} — TOTP disabled")
     except Exception as e:
         logger.warning(f"TOTP health check failed: {e} — TOTP disabled")
     ```
   - Then proceed to start the daemon (daemon.run() or equivalent).
3. Ensure any existing code that referred to `totp_enabled` from command line now relies on the daemon instance attribute set by health check.
4. Update any help text or documentation references in the CLI to remove `--totp` mentions.
</action>
<acceptance_criteria>
- VPNDaemon.__init__ does NOT have a `totp_enabled` parameter; instead `self.totp_enabled = False` is set inside __init__
- daemon.py main() no longer parses `--totp` argument
- main() calls `totp_service.health_check()` before starting daemon
- daemon.totp_enabled is set based on health check result
- Logging includes info when TOTP enabled, warning when disabled
- `proton-vpn-manager --help` does NOT show `--totp` option
</acceptance_criteria>
</task>

<task id="3" type="auto">
<description>Update adapter spawn to use TOTP service based on daemon's totp_enabled</description>
<read_first>
- multi-tunnel-namespace/src/daemon/daemon.py (_spawn_adapter method)
</read_first>
<action>
1. In `_spawn_adapter`, before building startup_payload:
   - If `self.totp_enabled` is True:
     - Attempt to fetch TOTP secret: `raw_secret = await totp_service.get_user_totp_secret(vpn_username)`
     - Convert to base64: `totp_secret = base64.b64encode(raw_secret).decode('ascii')`
     - On any exception: log warning, set `totp_secret = None`, and proceed without TOTP for this adapter
   - If `self.totp_enabled` is False: `totp_secret = None`
2. Include `"totp_secret": totp_secret` in the startup_payload dict (can be None).
3. Ensure imports: `import base64`, `from . import totp_service`
4. Keep existing error handling: if fetching fails, log and continue (adapter will operate without TOTP secret and will not perform TOTP verification).
</action>
<acceptance_criteria>
- `_spawn_adapter` contains conditional `if self.totp_enabled` block calling `totp_service.get_user_totp_secret`
- Catches exceptions from totp_service and logs warning
- `startup_payload` includes `"totp_secret": totp_secret` (can be None)
- No `totp_enabled` argument passed to VPNDaemon constructor
</acceptance_criteria>
</task>

<task id="4" type="auto">
<description>Ensure graceful degradation when TOTP service fails after startup</description>
<read_first>
- multi-tunnel-namespace/src/daemon/daemon.py (all adapter management methods)
</read_first>
<action>
1. Review code paths where `totp_service.get_user_totp_secret` is called (currently only in _spawn_adapter). Ensure:
   - If service becomes unreachable after daemon startup, later adapter spawns fail gracefully (totp_secret = None, adapter starts without TOTP).
   - No hard dependency on TOTP service availability after initial health check.
2. Add retry logic with backoff if desired (optional): if health check at startup failed but we want to retry periodically? For now, keep simple: daemon startup fixes totp_enabled; no dynamic updates.
3. Ensure logs clearly indicate when TOTP mode is active vs disabled for each adapter spawn.
</action>
<acceptance_criteria>
- Code review confirms no hard failures if TOTP service throws exception during get_user_totp_secret
- Adapter can still spawn with totp_secret=None (TOTP verification will be skipped downstream)
- Acceptance criteria can be verified by reading code comments and exception handling
</acceptance_criteria>
</task>
</tasks>

<verification>
After this plan:
1. Daemon starts without `--totp` flag; `--help` does not mention TOTP flag.
2. At startup, daemon performs TOTP service health check; `daemon.totp_enabled` is True if service reachable, False otherwise.
3. Adapter spawns include `totp_secret` in stdin payload when `totp_enabled` is True; else `totp_secret` is null/absent.
4. No command-line flag required; behavior auto-detected.
</verification>