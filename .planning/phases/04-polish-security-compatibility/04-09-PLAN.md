---
wave: 9
depends_on:
  - "04-08"
files_modified:
  - docs/ARCHITECTURE.md
  - docs/developer/MIGRATION.md
  - docs/developer/ADAPTER_IMPLEMENTATION_GUIDE.md
  - docs/developer/PROTOCOL_SPECS.md
  - docs/developer/TESTING_STRATEGIES.md
autonomous: false
must_haves:
  truths:
    - "Architecture documentation covers all components and security model"
    - "Migration guide explains session persistence removal and TOTP changes"
    - "Developer adapter guide provides implementation checklist and protocol specs"
    - "Testing strategies document includes Phase 4 test approach"
  artifacts:
    - path: docs/ARCHITECTURE.md
      provides: "Consolidated architecture overview with component diagram, TOTP, socket security, legacy forwarding"
      min_lines: 2000
    - path: docs/developer/MIGRATION.md
      provides: "Migration guide from old to new architecture (session persistence removal, 2FA mandatory)"
      min_lines: 1000
    - path: docs/developer/ADAPTER_IMPLEMENTATION_GUIDE.md
      provides: "Step-by-step adapter implementation guide with TOTP encryption and crypto patterns"
      min_lines: 1500
    - path: docs/developer/PROTOCOL_SPECS.md
      provides: "Formal protocol specifications for all communication channels including TOTP envelope"
      min_lines: 1000
    - path: docs/developer/TESTING_STRATEGIES.md
      provides: "Testing approach for integration, TOTP, legacy API, and stubs"
      min_lines: 800
  key_links:
    - from: ARCHITECTURE.md
      to: MIGRATION.md, ADAPTER_IMPLEMENTATION_GUIDE.md, PROTOCOL_SPECS.md, TESTING_STRATEGIES.md
      via: "references detailed specs"
---

<tasks>
<task id="1" type="auto">
<description>Create consolidated ARCHITECTURE.md merging prior specs</description>
<read_first>
- docs/NEW_ADAPTER_ARCHITECTURE_SPECIFICATION.md
- docs/ADAPTER_INTEGRATION.md
- docs/DBUS_SECURITY_AND_ARCHITECTURE.md
- docs/2FA_AUTHENTICATION.md
- .planning/phases/04-polish-security-compatibility/04-RESEARCH.md (architecture synthesis)
</read_first>
<action>
1. Create `docs/ARCHITECTURE.md` with the following structure:
   - **Multi-Tunnel Adapter Architecture** (overview, component diagram)
   - **Core Components**: MTM daemon, adapters, CLI client, D-Bus service
   - **Adapter Lifecycle**: Start, register, create tunnel, stop, crash recovery
   - **Communication Channels**:
     * CLI↔MTM (D-Bus)
     * MTM↔Adapter (control channel, with optional TOTP encryption)
     * CLI↔Adapter (tunnel operations, with optional TOTP encryption)
   - **Security Model**:
     * Socket permissions (0600, 0660)
     * SO_PEERCRED for control socket authentication
     * Credential delivery via stdin (not environment)
     * Memory sanitation (zeroing)
     * TOTP two-layer authentication (MTM local + VPN backend optional)
   - **Legacy Compatibility**: D-Bus forwarding approach
   - **Configuration**: TOTP service auto-discovery, environment variables, config file
   - **Cross-references**: link to detailed specs (2FA_AUTHENTICATION.md, PROTOCOL_SPECS.md, etc.)
2. Synthesize information from the source docs; do not copy verbatim but condense and clarify. Aim for 2000+ words.
3. Include code snippets where helpful (e.g., socket setup, TOTP detection, forwarding).
4. Use diagrams as ASCII/mermaid if needed.
5. Ensure all Phase 4 decisions (automatic TOTP mode, no --totp flag, encryption stub) are documented.
</action>
<acceptance_criteria>
- `docs/ARCHITECTURE.md` exists with at least 2000 words.
- Contains sections as listed above.
- Mentions TOTP auto-detection (no command-line flag).
- Describes socket permissions and SO_PEERCRED.
- Explains legacy D-Bus forwarding.
- Includes at least one diagram (ASCII/mermaid) showing component interactions.
- References `PROTOCOL_SPECS.md`, `2FA_AUTHENTICATION.md`, etc.
</acceptance_criteria>
</task>

<task id="2" type="auto">
<description>Create developer migration guide (docs/developer/MIGRATION.md)</description>
<read_first>
- .planning/phases/04-polish-security-compatibility/04-CONTEXT.md (migration context)
- existing user/OVERVIEW.md (to understand user perspective)
</read_first>
<action>
1. Create `docs/developer/MIGRATION.md` targeting developers/administrators transitioning from the old single-process architecture to the new multi-tunnel system.
2. Structure:
   - **Summary of Changes**: What's different (adapter processes, credential flow, 2FA, namespace isolation)
   - **Session Persistence Removal**: Explain why credentials no longer stored on disk; users must re-login after each adapter restart.
   - **TOTP Two-Factor**: Local MTM 2FA (auto-enabled if TOTP service reachable) and optional VPN backend 2FA.
   - **Adapter Management**: New `protonvpn adapter` commands (list, stop).
   - **Configuration Changes**: TOTP service discovery via environment/config; no `--totp` flag.
   - **Scripting Impact**: Changes to automation, environment expectations.
   - **Troubleshooting**: Common issues (TOTP service unreachable, socket permissions, namespace cleanup).
   - **Rollback**: How to revert to previous version if needed.
3. Be explicit: "Session persistence removed — you must log in after each adapter restart."
4. Mention TOTP mode: "MTM 2FA is automatically enabled if the TOTP service is reachable; otherwise it operates without 2FA."
5. Include examples of old vs new command invocations.
6. Length: at least 1000 words.
</action>
<acceptance_criteria>
- `docs/developer/MIGRATION.md` exists with required sections.
- Explicitly states "Session persistence removed; you must log in after each adapter restart."
- Mentions automatic TOTP mode detection (no --totp flag).
- Contains at least 1000 words.
- Cross-references ARCHITECTURE.md and ADAPTER_IMPLEMENTATION_GUIDE.md.
</acceptance_criteria>
</task>

<task id="3" type="auto">
<description>Create/update developer adapter implementation guide</description>
<read_first>
- docs/ADAPTER_INTEGRATION.md (existing guide)
- .planning/phases/04-polish-security-compatibility/04-CONTEXT.md (adapter decisions)
- docs/2FA_AUTHENTICATION.md (TOTP integration)
</read_first>
<action>
1. If `docs/developer/ADAPTER_IMPLEMENTATION_GUIDE.md` already exists from earlier phases, enhance it. Otherwise create new.
2. Ensure the guide includes:
   - **Overview**: Adapter purpose, lifecycle
   - **Project Setup**: Python, dependencies, entry point
   - **Dual-Server Pattern**: CLI socket and control connection
   - **Startup Sequence**: Receive stdin credentials (including optional totp_secret), connect to MTM control, register
   - **Control Protocol**: Register, AllocateTunnel, ReleaseTunnel message formats (include TOTP encryption envelope)
   - **TOTP Encryption**: How to encrypt/decrypt using totp_crypto, handling totp_secret
   - **Tunnel Management**: Creating, destroying, tracking multiple tunnels
   - **Crash Handling**: Signal handling, cleanup
   - **Testing**: Unit vs integration, using the dummy adapter as reference
   - **Debugging Tips**: Logging, socket inspection, namespace checks
   - **Checklist Before Submit**: All requirements from DAEM/ADPT/CLI/SEC coverage
3. Include concrete code snippets for critical parts (startup, control send/receive, TOTP usage).
4. Reference `PROTOCOL_SPECS.md` for formal message formats.
5. Ensure length >1500 words.
</action>
<acceptance_criteria>
- File exists with comprehensive sections as above.
- Includes code examples for TOTP encryption integration.
- Mentions automatic TOTP mode and totp_secret may be None.
- At least 1500 words.
- References PROTOCOL_SPECS.md and ARCHITECTURE.md.
</acceptance_criteria>
</task>

<task id="4" type="auto">
<description>Create/update protocol specifications with TOTP envelope</description>
<read_first>
- existing docs/developer/PROTOCOL_SPECS.md if present
- 04-RESEARCH.md (channel encryption patterns)
</read_first>
<action>
1. Create or update `docs/developer/PROTOCOL_SPECS.md`.
2. Document formal message formats for all channels:
   - **MTM↔Adapter Control Channel**:
     * Request envelope: `{"totp": "<6-digit>", "encrypted": "<base64 ciphertext>"}` with plaintext JSON inside
     * Response envelope: `{"totp": "<6-digit>", "encrypted": "<base64>"}` (or `{"error": ...}`)
     * Plaintext request/response schema: message types, fields, error codes
   - **CLI↔Adapter Channel**:
     * Request envelope same structure; includes totp code if TOTP enabled
     * Response envelope similar
     * Plaintext commands: CreateTunnel, DestroyTunnel, etc.
   - **D-Bus API** (legacy and new): method signatures, parameter types, return types
3. Include examples for each message type, both plaintext and encrypted forms.
4. Describe key derivation: TOTP code → 16-byte key via `_totp_to_key` (XOR stub; production upgrade path).
5. Document fallback behavior when TOTP disabled (no envelope, plaintext).
6. Reference `totp_crypto` module as implementation.
7. Ensure at least 1000 words.
</action>
<acceptance_criteria>
- `docs/developer/PROTOCOL_SPECS.md` exists with channel specifications.
- Includes TOTP envelope format for both control and CLI channels.
- Shows example encrypted and unencrypted messages.
- Describes key derivation and encryption/decryption functions.
- At least 1000 words.
</acceptance_criteria>
</task>

<task id="5" type="auto">
<description>Create/update testing strategies document</description>
<read_first>
- existing docs/developer/TESTING_STRATEGIES.md if present
- 04-RESEARCH.md (testing recommendations)
</read_first>
<action>
1. Create or update `docs/developer/TESTING_STRATEGIES.md`.
2. Cover:
   - **Test Pyramid**: unit, integration, system tests
   - **Fixtures**: daemon, manager_client, adapter fixtures (lifecycle management)
   - **Mocking vs Real**: What to mock (Proton API) vs what to test integrally (namespace creation)
   - **Testing TOTP Encryption**: Using fixed TOTP secrets, time control, validating round-trip
   - **Testing Legacy Forwarding**: Calling D-Bus methods and verifying adapter receives correct messages
   - **Testing Socket Permissions**: Using `os.stat` to check mode
   - **Testing Credential Sanitation**: Heuristic (code review); runtime check via `/proc/<pid>/environ` for absence of secrets
   - **Testing Crash Recovery**: Sending `SIGKILL` to adapter and verifying namespace cleanup within timeout
   - **Testing Concurrent Access**: Using asyncio.gather to simulate multiple clients
   - **Phase 4 Stubs**: Explain that test stubs are placeholders; full tests to be written after implementation
   - **Running Tests**: pytest commands, integration test markers, required privileges (root for netns)
3. Provide example test snippets for critical paths.
4. Length: ~800+ words.
</action>
<acceptance_criteria>
- `docs/developer/TESTING_STRATEGIES.md` exists with sections as above.
- Includes concrete examples (code snippets) for TOTP, legacy, permissions, crash.
- Mentions test stubs from Phase 4 and their purpose.
- At least 800 words.
</acceptance_criteria>
</task>

<task id="6" type="auto">
<description>Add initial docs/user/OVERVIEW.md if not present</description>
<read_first>
- docs/ directory listing
</read_first>
<action>
1. If `docs/user/OVERVIEW.md` does not exist, create a user-facing overview:
   - What is Multi-Tunnel Manager?
   - Key features: multiple simultaneous VPN tunnels, isolation, security
   - How TOTP 2FA works (high-level, non-technical)
   - Basic usage: `protonvpn tunnel create`, `protonvpn adapter list`
   - Migration from old version (link to developer/MIGRATION.md for details)
   - Security considerations
2. Keep it user-friendly, ~500-1000 words.
3. Save as `docs/user/OVERVIEW.md`.
</action>
<acceptance_criteria>
- `docs/user/OVERVIEW.md` exists if it was missing.
- Provides high-level user introduction.
- Links to full docs.
- At least 500 words.
</acceptance_criteria>
</task>
</tasks>

<verification>
After this plan:
1. All key documentation files exist:
   - `docs/ARCHITECTURE.md`
   - `docs/developer/MIGRATION.md`
   - `docs/developer/ADAPTER_IMPLEMENTATION_GUIDE.md`
   - `docs/developer/PROTOCOL_SPECS.md`
   - `docs/developer/TESTING_STRATEGIES.md`
   - `docs/user/OVERVIEW.md`
2. DOC-01, DOC-02, DOC-03 are satisfied.
3. Each doc meets minimum length and content requirements.
4. Cross-references are established.
</verification>